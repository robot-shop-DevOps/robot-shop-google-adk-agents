apiVersion: serving.knative.dev/v1

kind: Service

metadata:
  name: gcp-infra-agent
  
spec:
  template:
    spec:
      containers:
      - image: ${IMAGE}
        ports:
        - containerPort: 9000